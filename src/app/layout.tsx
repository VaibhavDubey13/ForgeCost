import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MatCost — Material Cost Calculator for Tradespeople",
  description:
    "Stop undercharging. MatCost helps plumbers, electricians, handymen, and other tradespeople calculate exact material costs with markup — and generate professional PDF quotes instantly.",
  keywords: [
    "material cost calculator",
    "tradesperson quote",
    "plumber estimate",
    "electrician quote",
    "contractor pricing",
  ],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen bg-background`}
      >
        {children}
      </body>
    </html>
  );
}
